<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaozhidao.mapper.HospitalDepartmentMapper">

    <select id="findByHospitalId" resultType="com.yaozhidao.entity.HospitalDepartment">
        SELECT id, hospital_id, department_name, description, is_main
        FROM hospital_department
        WHERE hospital_id = #{hospitalId}
        ORDER BY is_main DESC, id
    </select>

</mapper>
