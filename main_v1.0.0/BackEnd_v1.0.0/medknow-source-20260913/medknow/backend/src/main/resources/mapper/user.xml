<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaozhidao.mapper.UserMapper">

    <sql id="cols">
        id, phone, user_name, avatar, age, gender, occupation, allergies,
        chronic_diseases, status, theme_color, notification_enabled, sms_reminder_enabled,
        created_at, updated_at
    </sql>

    <select id="findById" resultType="com.yaozhidao.entity.User">
        SELECT
        <include refid="cols"/>
        FROM user WHERE id = #{id}
    </select>

    <select id="findByPhone" resultType="com.yaozhidao.entity.User">
        SELECT
        <include refid="cols"/>
        FROM user WHERE phone = #{phone}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (phone, user_name, avatar, status, theme_color, notification_enabled, sms_reminder_enabled)
        VALUES (#{phone}, #{userName}, #{avatar}, #{status}, #{themeColor}, #{notificationEnabled}, #{smsReminderEnabled})
    </insert>

    <!-- 更新个人资料：仅更新非 null 字段；status 由服务端计算后总是更新 -->
    <update id="updateProfile">
        UPDATE user
        <set>
            <if test="userName != null">user_name = #{userName},</if>
            <if test="age != null">age = #{age},</if>
            <if test="gender != null">gender = #{gender},</if>
            <if test="occupation != null">occupation = #{occupation},</if>
            <if test="allergies != null">allergies = #{allergies},</if>
            <if test="chronicDiseases != null">chronic_diseases = #{chronicDiseases},</if>
            status = #{status},
        </set>
        WHERE id = #{id}
    </update>

    <!-- 更新偏好设置：仅更新非 null 字段 -->
    <update id="updateSettings">
        UPDATE user
        <set>
            <if test="themeColor != null">theme_color = #{themeColor},</if>
            <if test="notificationEnabled != null">notification_enabled = #{notificationEnabled},</if>
            <if test="smsReminderEnabled != null">sms_reminder_enabled = #{smsReminderEnabled},</if>
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE user SET avatar = #{avatar} WHERE id = #{id}
    </update>

</mapper>
