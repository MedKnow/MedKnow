<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yaozhidao.mapper.DrugMapper">

    <sql id="searchCols">
        id, drug_name, generic_name, category, summary
    </sql>

    <!-- 模糊搜索：参数化 LIKE 防注入；商品名/通用名双匹配 -->
    <select id="search" resultType="com.yaozhidao.entity.Drug">
        SELECT
        <include refid="searchCols"/>
        FROM drug
        WHERE drug_name LIKE CONCAT('%', #{keyword}, '%')
           OR generic_name LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY drug_name
        LIMIT #{offset}, #{size}
    </select>

    <select id="countSearch" resultType="long">
        SELECT COUNT(*) FROM drug
        WHERE drug_name LIKE CONCAT('%', #{keyword}, '%')
           OR generic_name LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <select id="findById" resultType="com.yaozhidao.entity.Drug">
        SELECT id, drug_name, generic_name, category, summary, indications, dosage,
               side_effects, contraindications, precautions, storage,
               max_single_dose, max_dose_unit
        FROM drug WHERE id = #{id}
    </select>

    <!-- 按名称匹配药品库：精确匹配优先（verified 判断），模糊匹配兜底 -->
    <select id="findByName" resultType="com.yaozhidao.entity.Drug">
        SELECT id, drug_name, generic_name, category, summary, indications, dosage,
               side_effects, contraindications, precautions, storage,
               max_single_dose, max_dose_unit
        FROM drug
        WHERE drug_name LIKE CONCAT('%', #{name}, '%')
           OR generic_name LIKE CONCAT('%', #{name}, '%')
        ORDER BY CASE WHEN drug_name = #{name} OR generic_name = #{name} THEN 0 ELSE 1 END, id
        LIMIT 5
    </select>

</mapper>
